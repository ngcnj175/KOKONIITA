{
  "name": "kokoniita-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute kokoniita --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute kokoniita --file=./schema.sql --local",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
